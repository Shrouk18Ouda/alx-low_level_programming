#include<stdio.h>
/**
 * main - Entry point
 *
 * Description:print nums from 0 to 9 with for
 *
 * Return: Always 0 (success)
*/
int main(void)
{
	char ch;

	for (ch = 0; ch <= 9; ch++)
	putchar(ch);
	putchar('\n');
	return (0);
}
