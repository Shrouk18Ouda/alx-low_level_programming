#include<stdio.h>
/**
 * main - Entry point
 *
 * Description:print nums from 0 to 9 with for
 *
 * Return: Always 0 (success)
*/
int main(void)
{
	int ch;

	for (ch = 48; ch <= 57; ch++)
	putchar(ch);
	putchar('\n');
	return (0);
}
